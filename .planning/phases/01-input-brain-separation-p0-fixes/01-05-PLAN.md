---
phase: 01-input-brain-separation-p0-fixes
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - Assets/Scripts/Player/PlayerInput.cs
  - Assets/Scripts/Player/Movement/GroundChecker.cs
  - Assets/Scripts/Player/Movement/PlayerWallRun.cs
  - Assets/Scripts/Player/Movement/States/WallRunningState.cs
  - Assets/Scripts/Core/FSM/StateMachine.cs
  - Assets/Scripts/Core/FSM/BaseState.cs
  - Assets/Scripts/Player/Movement/MovementState.cs
autonomous: true
requirements:
  - ARCH-05
  - ARCH-06
  - ARCH-09
  - ARCH-10

must_haves:
  truths:
    - "Camera.main is not called in PlayerInput.Update() — it is cached in Awake() as _mainCamera"
    - "Debug.DrawLine and Debug.DrawRay in GroundChecker and PlayerWallRun are wrapped in #if UNITY_EDITOR"
    - "No Spanish characters (á é í ó ú ñ Á É Í Ó Ú Ñ) appear anywhere in Assets/Scripts/"
    - "All PlayerGrapple tuning values remain accessible via [SerializeField] — no raw float literals in gameplay methods"
  artifacts:
    - path: "Assets/Scripts/Player/PlayerInput.cs"
      provides: "Cached _mainCamera field used in CalculateCameraRelativeDirection (or Brain equivalent)"
      contains: "_mainCamera"
    - path: "Assets/Scripts/Player/Movement/GroundChecker.cs"
      provides: "Debug.DrawLine calls gated behind #if UNITY_EDITOR"
      contains: "#if UNITY_EDITOR"
    - path: "Assets/Scripts/Player/Movement/PlayerWallRun.cs"
      provides: "Debug.DrawRay calls gated behind #if UNITY_EDITOR"
      contains: "#if UNITY_EDITOR"
  key_links:
    - from: "Assets/Scripts/Player/PlayerInput.cs"
      to: "Unity Camera system"
      via: "_mainCamera cached in Awake(), used in Update() path for direction calculation"
      pattern: "_mainCamera"
---

<objective>
Four mechanical cleanup tasks — no logic changes, no new files except minor field additions. All are independent of each other within this plan.

Task 1 (ARCH-09): Cache Camera.main in PlayerInput.Awake() as _mainCamera. Replace the uncached Camera.main call in the Update/direction-calculation path.

Task 2 (ARCH-10): Wrap Debug.DrawLine calls in GroundChecker.cs (lines ~95, ~115) and Debug.DrawRay calls in PlayerWallRun.cs (lines ~100–103) in #if UNITY_EDITOR / #endif.

Task 3 (ARCH-06): Replace all Spanish comments and Debug.Log/LogError strings throughout affected files. Files confirmed to have Spanish content: WallRunningState.cs, StateMachine.cs, PlayerInput.cs, BaseState.cs, MovementState.cs (context class comment).

Task 4 (ARCH-05): Audit PlayerGrapple.cs and all movement components for raw float literals in gameplay method bodies. Expose any found values via [SerializeField] private fields. Research confirmed PlayerGrapple.cs already uses [SerializeField] for all tuning values — this task is primarily a verification pass with targeted fixes only where literals are found.

Purpose: Satisfies ARCH-05, ARCH-06, ARCH-09, ARCH-10.
Output: PlayerInput.cs (camera cached). GroundChecker.cs and PlayerWallRun.cs (debug draws gated). Several files (Spanish comments replaced). No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-input-brain-separation-p0-fixes/01-RESEARCH.md
</context>

<interfaces>
<!-- Patterns to fix — read each file before editing. -->

PlayerInput.cs — Camera.main caching (ARCH-09):
```csharp
// Current broken (in Update path — line 143 area):
var cam = Camera.main;  // O(n) search every frame

// Fix: add field + cache in Awake:
private Camera _mainCamera;
// In Awake(): _mainCamera = Camera.main;
// In Update path: replace 'Camera.main' or 'cam = Camera.main' with '_mainCamera'
```

GroundChecker.cs — Debug.DrawLine wrapping (ARCH-10):
```csharp
// Current broken:
Debug.DrawLine(transform.position, transform.position + Vector3.down * groundCheckDistance, Color.red);

// Fix: wrap in preprocessor directive:
#if UNITY_EDITOR
Debug.DrawLine(transform.position, transform.position + Vector3.down * groundCheckDistance, Color.red);
#endif
```

PlayerWallRun.cs — Debug.DrawRay wrapping (ARCH-10):
```csharp
// Current broken (lines ~100-103):
Debug.DrawRay(transform.position, transform.right * wallCheckDistance, Color.green);
// (similar calls)

// Fix: wrap entire block in #if UNITY_EDITOR / #endif
```

Spanish comment locations confirmed by research:
- WallRunningState.cs: section comments throughout OnUpdate (lines 16–63)
- StateMachine.cs: lines 17, 47, 54, 84 — error strings and inline comments
- PlayerInput.cs: line 38 (Debug.LogError string in Spanish), line 48 (// Crear contexto)
- BaseState.cs: line 15 (// Métodos virtuales con implementación por defecto)
- MovementState.cs: Spanish comments in PlayerMovementContext class body
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cache Camera.main in PlayerInput.Awake() and replace per-frame call</name>
  <files>Assets/Scripts/Player/PlayerInput.cs</files>
  <read_first>
    - Assets/Scripts/Player/PlayerInput.cs (full file — after Plan 01 edits. Find every occurrence of Camera.main, find Awake(), confirm _mainCamera does not already exist)
  </read_first>
  <action>
Note: Plan 01 extracted CalculateCameraRelativeDirection logic to PlayerMovementBrain. Read PlayerInput.cs in its POST-Plan-01 state carefully — the Camera.main call may now be in PlayerMovementBrain.cs instead. If so:

Scenario A — Camera.main is in PlayerMovementBrain.cs (moved by Plan 01):
- Add `private Camera _mainCamera;` field to PlayerMovementBrain.cs
- Add `_mainCamera = Camera.main;` in PlayerMovementBrain.Awake()
- Replace the `Camera.main` call in ProcessFrame or camera-direction helper with `_mainCamera`
- File modified: Assets/Scripts/Player/PlayerMovementBrain.cs (add to files_modified if needed)

Scenario B — Camera.main is still in PlayerInput.cs:
- Add `private Camera _mainCamera;` field to PlayerInput
- Add `_mainCamera = Camera.main;` in PlayerInput.Awake()
- Replace the per-frame `Camera.main` or `var cam = Camera.main` with `_mainCamera`

In either case, the goal is: zero calls to `Camera.main` in any Update() or FixedUpdate() tick path. One call only in Awake().

After the fix: `grep -n "Camera.main" Assets/Scripts/Player/PlayerInput.cs` should return 0 results. If the camera math moved to PlayerMovementBrain, also check: `grep -n "Camera.main" Assets/Scripts/Player/PlayerMovementBrain.cs` should return 0 (only _mainCamera usage remains).
  </action>
  <verify>
    <automated>grep -n "Camera.main" "Assets/Scripts/Player/PlayerInput.cs" "Assets/Scripts/Player/PlayerMovementBrain.cs" 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Camera.main" Assets/Scripts/Player/PlayerInput.cs` returns 0
    - `grep -c "_mainCamera" Assets/Scripts/Player/PlayerInput.cs` returns at least 1 OR `grep -c "_mainCamera" Assets/Scripts/Player/PlayerMovementBrain.cs` returns at least 1 (cached in whichever file holds the camera math)
    - The file with _mainCamera has `_mainCamera = Camera.main` exactly once — in Awake(), not in Update() or FixedUpdate()
    - `grep -rn "Camera\.main" Assets/Scripts/Player/` returns 0 results (no per-frame calls anywhere in Player/)
  </acceptance_criteria>
  <done>Camera.main cached at startup. Zero per-frame Camera.main calls in PlayerInput or PlayerMovementBrain. ARCH-09 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Gate Debug.DrawLine and Debug.DrawRay behind #if UNITY_EDITOR in GroundChecker and PlayerWallRun</name>
  <files>
    Assets/Scripts/Player/Movement/GroundChecker.cs
    Assets/Scripts/Player/Movement/PlayerWallRun.cs
  </files>
  <read_first>
    - Assets/Scripts/Player/Movement/GroundChecker.cs (full file — find Debug.DrawLine calls, note line numbers and surrounding context)
    - Assets/Scripts/Player/Movement/PlayerWallRun.cs (full file — find Debug.DrawRay calls, note their surrounding context)
  </read_first>
  <action>
For GroundChecker.cs:
Find each `Debug.DrawLine(...)` call. For each one, wrap it:
```csharp
#if UNITY_EDITOR
Debug.DrawLine(...);
#endif
```
If multiple DrawLine calls are adjacent or within the same short block, wrap the entire block in a single #if/#endif rather than wrapping each individually.

Do NOT wrap the surrounding method body — only the DrawLine statement(s).

For PlayerWallRun.cs:
Find each `Debug.DrawRay(...)` call (research notes ~lines 100–103). Wrap with:
```csharp
#if UNITY_EDITOR
Debug.DrawRay(...);
// (additional adjacent DrawRay calls)
#endif
```
Same rule: wrap the minimal block containing only DrawRay calls.

Do not wrap Debug.Log calls — only DrawLine and DrawRay are in scope for ARCH-10.
Do not reformat surrounding code.
  </action>
  <verify>
    <automated>grep -n "Debug.DrawLine\|Debug.DrawRay\|#if UNITY_EDITOR" "Assets/Scripts/Player/Movement/GroundChecker.cs" "Assets/Scripts/Player/Movement/PlayerWallRun.cs"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Debug.DrawLine" Assets/Scripts/Player/Movement/GroundChecker.cs` returns at least 1 (calls still present)
    - For every DrawLine call in GroundChecker.cs, there is a `#if UNITY_EDITOR` on a preceding line with no intervening DrawLine calls (i.e. the call is inside the directive)
    - `grep -c "Debug.DrawRay" Assets/Scripts/Player/Movement/PlayerWallRun.cs` returns at least 1
    - For every DrawRay call in PlayerWallRun.cs, there is a `#if UNITY_EDITOR` on a preceding line
    - `grep -c "#endif" Assets/Scripts/Player/Movement/GroundChecker.cs` returns at least 1
    - `grep -c "#endif" Assets/Scripts/Player/Movement/PlayerWallRun.cs` returns at least 1
    - Both files compile without errors
  </acceptance_criteria>
  <done>Debug.DrawLine in GroundChecker and Debug.DrawRay in PlayerWallRun are inside #if UNITY_EDITOR blocks. No debug draw overhead in player builds. ARCH-10 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Replace all Spanish comments and strings across affected files</name>
  <files>
    Assets/Scripts/Player/Movement/States/WallRunningState.cs
    Assets/Scripts/Core/FSM/StateMachine.cs
    Assets/Scripts/Player/PlayerInput.cs
    Assets/Scripts/Core/FSM/BaseState.cs
    Assets/Scripts/Player/Movement/MovementState.cs
  </files>
  <read_first>
    - Assets/Scripts/Player/Movement/States/WallRunningState.cs (full file — find Spanish section comments in OnUpdate)
    - Assets/Scripts/Core/FSM/StateMachine.cs (full file — find Spanish error strings at lines 17, 47, 54, 84)
    - Assets/Scripts/Player/PlayerInput.cs (full file — find Spanish at line 38 Debug.LogError string and line 48 comment)
    - Assets/Scripts/Core/FSM/BaseState.cs (full file — find Spanish at line 15)
    - Assets/Scripts/Player/Movement/MovementState.cs (full file — find Spanish comments in PlayerMovementContext class)
  </read_first>
  <action>
For each file, read the file and find every line containing Spanish characters (á, é, í, ó, ú, ñ, Á, É, Í, Ó, Ú, Ñ) or Spanish words in comments/strings. Replace each with an English equivalent that preserves the meaning.

Specific translations needed (read the file to verify these are the actual lines):

WallRunningState.cs (OnUpdate section comments):
- `// Salir si ya no está corriendo en la pared` → `// Exit if no longer running on the wall`
- `// Manejar salto desde la pared` → `// Handle wall jump`
- Any other Spanish section comments → translate to equivalent English

StateMachine.cs:
- Line 17 area: Spanish error string in Initialize() — translate to English
- Line 47 area: Spanish string in ChangeState() — translate
- Line 54 area: Spanish inline comment — translate
- Line 84 area: Spanish comment in Clear() — translate

PlayerInput.cs (in its post-Plan-01 state — some lines may have shifted):
- Spanish Debug.LogError string → translate to English
- `// Crear contexto` → `// Create context` (if this line still exists after Plan 01 extraction)

BaseState.cs:
- Line 15: `// Métodos virtuales con implementación por defecto` → `// Virtual methods with default implementations`

MovementState.cs:
- Any Spanish comments in PlayerMovementContext class → translate to English
- Note: `// Referencias a componentes` → `// Component references`
- Note: `// Lo guardo para que no cambie una vez que lo ejecuto` → `// Stored so it does not change once the dash executes`

After all replacements, run:
`grep -rn "[áéíóúñÁÉÍÓÚÑ]" Assets/Scripts/`
This must return zero results.
  </action>
  <verify>
    <automated>grep -rn "[áéíóúñÁÉÍÓÚÑ]" "Assets/Scripts/"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "[áéíóúñÁÉÍÓÚÑ]" Assets/Scripts/` returns 0 results (zero Spanish characters anywhere in Assets/Scripts/)
    - All 5 modified files compile without errors
    - No identifier names were changed — only string literals and comments were modified
    - No logic was modified — only text content of comments and error strings
  </acceptance_criteria>
  <done>Zero Spanish characters remain in Assets/Scripts/. All comments and strings use English. ARCH-06 satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Audit and fix magic number literals in gameplay movement code (ARCH-05)</name>
  <files>Assets/Scripts/Player/Movement/PlayerGrapple.cs</files>
  <read_first>
    - Assets/Scripts/Player/Movement/PlayerGrapple.cs (full file — check every method body for raw float or int literals NOT covered by a [SerializeField] field)
    - Assets/Scripts/Player/Movement/PlayerMovement.cs (skim for hardcoded float literals in method bodies: CalculateHorizontalMovement, CalculateVerticalMovement, ApplyFinalVelocity)
    - Assets/Scripts/Player/Movement/PlayerJumper.cs (skim for hardcoded jump force or gravity literals)
    - Assets/Scripts/Player/Movement/PlayerDash.cs (skim for hardcoded dash distance or force literals)
  </read_first>
  <action>
This is a verification-first task. The research confirms PlayerGrapple.cs already uses [SerializeField] for all its tuning values. ARCH-05 for Phase 1 is satisfied by [SerializeField] exposure — full SO extraction is Phase 2 (MOVE-10).

For each file:
1. Scan all gameplay method bodies for raw numeric literals (e.g., `0.1f`, `2.0f`, `9.81f`, `500f`) that are NOT assigned from a [SerializeField] field.
2. If a literal is found in a gameplay method body (e.g., `velocity *= 0.85f;` with no corresponding field), ADD a `[SerializeField] private float fieldName = 0.85f;` field and replace the literal with the field reference.
3. If no unguarded literals are found in a file, note "no magic numbers found" and make no change.

Do NOT extract values into a ScriptableObject this phase — [SerializeField] is sufficient per ARCH-05 Phase 1 scope.

Focus on the four files listed. Do not scan the entire codebase — that is Phase 2 scope.

Naming convention for new fields: `[SerializeField] private float camelCaseName = defaultValue;` with a [Header] or [Tooltip] if the field's purpose is not obvious from its name.
  </action>
  <verify>
    <automated>grep -n "[SerializeField]\|[0-9]\+\.[0-9]\+f" "Assets/Scripts/Player/Movement/PlayerGrapple.cs" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - For each raw float literal found in gameplay method bodies (not in field declarations): a corresponding [SerializeField] field exists and the literal in the method body is replaced with the field reference
    - `grep -rn "[áéíóúñÁÉÍÓÚÑ]" Assets/Scripts/Player/Movement/PlayerGrapple.cs` returns 0 (no Spanish in this file either)
    - All modified files compile without errors
    - If no magic numbers were found: SUMMARY documents "ARCH-05 verified — no unguarded literals found in scanned files" and no code change is made
  </acceptance_criteria>
  <done>PlayerGrapple.cs and scanned movement components have all tuning values exposed via [SerializeField]. No raw float literals in gameplay method bodies. ARCH-05 satisfied for Phase 1 scope.</done>
</task>

</tasks>

<verification>
After all four tasks complete:

1. `grep -rn "Camera\.main" Assets/Scripts/Player/` — must return 0 results
2. `grep -n "Debug.DrawLine" Assets/Scripts/Player/Movement/GroundChecker.cs` — all results must be inside #if UNITY_EDITOR blocks
3. `grep -n "Debug.DrawRay" Assets/Scripts/Player/Movement/PlayerWallRun.cs` — all results must be inside #if UNITY_EDITOR blocks
4. `grep -rn "[áéíóúñÁÉÍÓÚÑ]" Assets/Scripts/` — must return 0 results
5. Unity Console: zero compile errors
</verification>

<success_criteria>
- Camera.main cached in Awake() — ARCH-09 satisfied
- Debug.DrawLine/DrawRay gated in editor — ARCH-10 satisfied
- Zero Spanish characters in codebase — ARCH-06 satisfied
- All PlayerGrapple tuning values accessible via [SerializeField] — ARCH-05 satisfied
- Zero compile errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-input-brain-separation-p0-fixes/01-05-SUMMARY.md`
</output>
